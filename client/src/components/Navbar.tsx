import { useNavigation } from "react-router-dom";

const Navbar = () => {
    const natvigation = useNavigation();

    return (
        <div>
            

        </div>        
     );
}

export default Navbar;